<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/welcome.css">
    <title>welcome-to-doc-platform</title>
</head>

<body>
    <div class="container">
        <div class="top-items">
            <img src="/images/usericon.png" width="80px" height="80px" alt="usericon">
            <p><%= user.name %></p>
            <a href="/login"><button class="signup" style="font-size: medium;">Sign Out</button></a>
        </div>

        <div class="heading1">
            <img src="/images/doc-icon.png" width="80px" height="80px" alt="">
            <h2>Welcome to Doc Platform</h2>
        </div>
        <div class="">
            <h1>Create a new workspace</h1>
            <form action="/dashboard" method="post">
                <div>
                    <input type="text" name="new_workspace" placeholder="Enter workspace name" autocomplete="off">
                </div>
                <input type="hidden" name="user_id" value="<%= user._id %>"></input>
                <div>
                    <button type="submit" class="btn-create">CREATE</button>
                </div>
            </form>
        </div>

        <% if(user.workspaces.length===0) { %>
            <h3></h3>
            <% } else { %>
                <h3>You have following workspaces</h3>
                <% for(var i=0; i<user.workspaces.length; i++) { %>
                    <div class="workspace-list">
                        <div>
                            <p class="workspace-name">
                                <%= user.workspaces[i].name %>
                            </p>
                        </div>
                        <div>
                            <a href="/user/<%= user._id %>/workspace/<%= user.workspaces[i]._id %>"><button class="workspace-btn">Open</button></a>
                            <a href="/userd/<%= user._id %>/workspaced/<%= user.workspaces[i]._id %>"><button class="workspace-btn" style="background-color: red;">Remove</button></a>
                        </div>
                    </div>
                <% } %>
        <% } %>
                            
    </div>
</body>

</html>